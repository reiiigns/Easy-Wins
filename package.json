{
  "name": "project-tracker",
  "private": true,
  "version": "0.4.2",
  "description": "Repository wrapper for Easy Wins Project Tracker.",
  "scripts": {
    "dev": "npm --prefix app run dev",
    "start": "npm --prefix app run start",
    "lint": "npm --prefix app run lint",
    "typecheck": "npm --prefix app run typecheck",
    "test": "npm --prefix app run test",
    "build": "npm --prefix app run build",
    "electron:build": "npm --prefix app run electron:build"
  }
}
